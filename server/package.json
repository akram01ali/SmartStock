{
  "name": "smartstock-server",
  "version": "1.0.0",
  "description": "SmartStock backend API",
  "main": "main.py",
  "scripts": {
    "db:studio": "prisma studio",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
